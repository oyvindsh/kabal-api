query($ident: ID!){
    hentPerson(ident: $ident) {
        navn {
            fornavn,
            mellomnavn,
            etternavn
        },
        folkeregisteridentifikator {
            identifikasjonsnummer,
            type,
            status
        }
    }
}
